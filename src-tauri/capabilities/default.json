{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the M0 desktop shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
